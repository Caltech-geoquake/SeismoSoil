% Last modified: 3/27/2013
\usepackage{amsmath,amssymb} % necessary for typesetting math formulae
%\usepackage{xfrac} % This package clashes with "kpfonts" package
\usepackage{extarrows}
\usepackage{dsfont}
\usepackage{cancel}   % Draw arrows accross math expressions to show that they vanish
% \cancel : draws a diagonal line (slash) through its argument.
% \bcancel: uses the negative slope (a backslash).
%\xcancel: draws an X (actually \cancel plus \bcancel).
% \cancelto{<value>}{<expression>}--draws a diagonal arrow through the ``expression'', pointing to the ``value''

%%% Simplifying Math Commands %%%
\newcommand{\f}[2]{\frac{#1}{#2}}
\newcommand{\lt}{\left}     % Left parenthesis, brackets or braces
\newcommand{\rt}{\right}    % Right parenthesis, brackets or braces
\newcommand{\la}{\langle}
\newcommand{\ra}{\rangle}

\newcommand{\ein}[1]{~\mathrm{#1}}  % units
      % Note on horizontal spaces:
      %  \, -- 3/18 of a quad, \: -- 4/18, \; -- 5/18, \! -- -3/18
\newcommand{\degr}{$^{\circ}$ }  % for use outside of math environments
\newcommand{\degree}{^{\circ}}   % for use inside of math environments

\newcommand{\udl}{\underline}
\newcommand{\ovl}{\overline}
\newcommand{\ud}{\,\mathrm{d}}
\newcommand{\dsp}{\displaystyle}
\newcommand{\6}{\partial}
\newcommand{\p}{\partial}

% \newcommand{\ra}{\rangle}
% \newcommand{\la}{\langle}
\newcommand{\mrm}{\mathrm}
\newcommand{\txt}[1]{\text{#1}}
\newcommand{\xeq}[1]{\stackrel{\;#1}{\cleaders\hbox{=\!}\hfill}\:} % extendible equal sign
\newcommand{\eqdef}{\xeq{\mathrm{def}}}
\newcommand{\epsl}{\varepsilon}
\newcommand{\0}{\theta}

\newcommand{\e}[1]{\times10^{#1}}

%%%%   Vectors and matrices   %%%%
%\newcommand{\vtr}{\vec}      % vector -- right arrow
%\newcommand{\vtr}{\mathbf}   % vector -- math bold font
\newcommand{\vtr}{\bm}
\newcommand{\h}{\hat}            % Definition of vector of unit length : hat
                                                            % Definition of ``matrix'' : left-right arrow
%\newcommand{\mat}[1]{\mathord{\buildrel{\lower3pt\hbox{$\scriptscriptstyle\leftrightarrow$}}\over{#1}}}
                                 % matrix -- left and right arrow
\newcommand{\mat}{\mathbf}       % matrix -- math bold font
\newcommand{\tr}{\;\!\!^{\mrm{T}}}  %  Definition of matrix/vector transposition

\newcommand{\omg}{\omega}
\newcommand{\Omg}{\Omega}

%%%%%%%%%%

\newcommand{\solution}{\noindent\underline{SOLUTION}:}
\newcommand{\prove}{\noindent\underline{PROVE}:}

\newcommand{\eqn}[1]{Eqn~\eqref{eq:#1}}
\newcommand{\Rightqrrow}{\Rightarrow}
\newcommand{\slashfrac}[2]{#1 \mathord{\left/ {\vphantom {#1 #2}} \right. \kern-\nulldelimiterspace} #2}

